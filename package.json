{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": {
    "name": "Angule Mathias",
    "email": "angulemathias3@gmail.com"
  },
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix api\" \"npm run dev --prefix app\"",
    "start": "concurrently \"npm start --prefix api\" \"npm start --prefix app\"",
    "build": "npm run build --prefix api && npm run build --prefix app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "license": "GNU",
  "dependencies": {
    "class-validator": "^0.14.1",
    "concurrently": "^8.2.2",
    "passport-jwt": "^4.0.1",
    "passport-local": "^1.0.0"
  }
}
